export type LoginData = {
    login: string;
    password: string;
}

export type RegisterData = {
    second_name: string;
    first_name: string;
    patronymic: string;
    telephone: string;
    email: string;
    login: string;
    password: string;
}